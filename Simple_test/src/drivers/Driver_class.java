package drivers;

import org.hibernate.Session;
import org.hibernate.SessionFactory;
import org.hibernate.Transaction;
import org.hibernate.cfg.Configuration;

import pojo.Market_Details;

public class Driver_class {

	public static void main(String[] args) {
     
		/*
		 * Configuration cfg=new Configuration();
		 * cfg.configure("configs/market.cfg.xml"); SessionFactory
		 * sf=cfg.buildSessionFactory(); Session s=sf.openSession(); Transaction
		 * t=s.beginTransaction(); Market_Details md1=new
		 * Market_Details("Amazon","big Billion day",850004654,86465421); Market_Details
		 * md2=new Market_Details("Flipkart","Homeshopping",335454554,1245421);
		 * System.out.println("market details created"); s.save(md1); s.save(md2);
		 * System.out.println("data saved"); t.commit(); s.close(); sf.close();
		 */
		
	}

}
